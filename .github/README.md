# GitHub Actions

Workflows for VaaniQ CI (ROADMAP-009):

| Workflow | Purpose |
|----------|---------|
| `ci.yml` | Backend, frontend, OpenAPI drift, Telugu guard, Docker builds |
| `docs.yml` | Phase 0 docs inventory presence check |
